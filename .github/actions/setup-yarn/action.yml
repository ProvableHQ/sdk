name: Setup Yarn
description: Sets up Yarn
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v6
      with:
        cache: 'yarn'

    - name: yarn install
      shell: bash
      run: |
        yarn install --immutable --check-cache
