# Aleo Loyalty Program - Environment Configuration
# Copy this file to .env and fill in your values.
# The .env file is automatically loaded via dotenv - no need to run "source .env".

# =============================================================================
# Proving Mode
# =============================================================================
# "local" (default) - Prove on this machine. Works with any program.
# "delegated" - Offload proving to a remote service. Requires deployed program.
ALEO_PROVING_MODE=local

# =============================================================================
# Delegated Proving Service (DPS)
# Required for: npm run delegated
# =============================================================================
# DPS URL (without network suffix - SDK adds /testnet or /mainnet automatically).
ALEO_DPS_URL=https://api.provable.com/prove

# API key for authentication.
ALEO_DPS_API_KEY=your-api-key-here

# Optional: Consumer ID for tracking.
# ALEO_DPS_CONSUMER_ID=

# Optional: Enable TEE-encrypted proving requests.
# ALEO_DPS_PRIVACY=true

# =============================================================================
# Record Scanning
# =============================================================================
# Scanner type: "rss" (default, faster, requires JWT) or "network" (slower, no auth)
# ALEO_SCANNER_TYPE=rss

# Block height to start scanning from (default: 0).
# Set this higher to skip older blocks and speed up scanning.
# ALEO_SCAN_START_HEIGHT=0

# =============================================================================
# RSS Scanner (RecordScanner Service)
# Required for: npm run scanner
# Faster, uses dedicated indexing service, requires JWT authentication.
# =============================================================================
# RSS URL (without network suffix - SDK adds /testnet or /mainnet automatically).
ALEO_RSS_URL=https://api.provable.com/scanner

# Consumer ID for fetching RSS JWT token (uses DPS API key to fetch JWT).
ALEO_RSS_CONSUMER_ID=your-consumer-id-here

# Or provide a JWT token directly (starts with "eyJ...").
# ALEO_RSS_API_KEY=eyJ...

# Optional: Enable TEE-encrypted record scanning.
# ALEO_RSS_PRIVACY=true

# =============================================================================
# Network Scanner (Explorer API)
# Required for: npm run scanner:network
# Slower, scans blocks directly via explorer API, no authentication needed.
# =============================================================================
# ALEO_NETWORK_URL=https://api.explorer.provable.com/v1
