# Aleo Loyalty Program - Environment Configuration
# Copy this file to .env and fill in your values.
# The .env file is automatically loaded via dotenv - no need to run "source .env".

# =============================================================================
# Authentication
# =============================================================================
# edge.provable.com/api (the default below) is unauthenticated. An API key is
# optional and, when set, is sent as an X-API-Key header.
#
# Only set a consumer ID when targeting the legacy api.provable.com gateway,
# which mints JWTs from the API key + consumer ID pair. edge has no JWT route.
# ALEO_CONSUMER_ID=your-consumer-id-here

# =============================================================================
# Proving Mode
# =============================================================================
# "local" (default) - Prove on this machine. Works with any program.
# "delegated" - Offload proving to a remote service. Requires deployed program.
ALEO_PROVING_MODE=local

# =============================================================================
# Delegated Proving Service (DPS)
# Required for: npm run delegated
# =============================================================================
# DPS URL (include the network suffix: /testnet or /mainnet).
ALEO_DPS_URL=https://edge.provable.com/api/prove/testnet

# Optional API key (edge accepts unauthenticated requests).
# ALEO_DPS_API_KEY=your-api-key-here

# Optional: Enable TEE-encrypted proving requests.
# ALEO_DPS_PRIVACY=true

# =============================================================================
# Record Scanning Service (RSS)
# Required for: npm run scanner
# Uses dedicated indexing service for faster record discovery.
# =============================================================================
# RSS URL (without network suffix - SDK adds /testnet or /mainnet automatically).
ALEO_RSS_URL=https://edge.provable.com/api/scanner

# Block height to start scanning from (default: 0).
# Set this higher to skip older blocks and speed up scanning.
# ALEO_SCAN_START_HEIGHT=0

# Optional API key for the scanner (X-API-Key), or a JWT token directly (starts with "eyJ...").
# ALEO_RSS_API_KEY=your-api-key-here
