# Web Keygen Template

Minimal web demo for `ProvableKit + WASM engine`.

## What it shows

- Explicit initialization via:
  - `ProvableKit.init({ engine: createWasmEngine(), env })`
- A worker-backed button flow that generates:
  - address
  - private key
  - view key

## Run

```bash
yarn install
yarn dev
```
