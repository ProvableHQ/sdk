{
    "name": "template-node-dynamic-dispatch-ts",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "build": "rimraf dist && rollup --config",
        "start": "npm run build && node dist/index.js",
        "test:dynamic": "npm run build && node dist/index.js dynamic_dispatch",
        "test:mixed": "npm run build && node dist/index.js mixed",
        "test:static": "npm run build && node dist/index.js static_imports",
        "test:chain": "npm run build && node dist/index.js deep_chain",
        "test:stress": "npm run build && node dist/index.js stress",
        "test:bench": "npm run build && node dist/index.js bench",
        "test:all": "npm run build && node dist/index.js"
    },
    "dependencies": {
        "@provablehq/sdk": "^0.10.0"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "rollup": "^4.59.0",
        "rollup-plugin-typescript2": "^0.36.0",
        "typescript": "^5.7.3"
    }
}
