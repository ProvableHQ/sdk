# Aleo + Node.js + TypeScript

`npm start`

This template explicitly initializes ProvableKit with the WASM engine via
`ProvableSDK.init({ engine: createWasmEngine(), env })` before running transaction flows.

Recommend Node.js 20+ for best performance.
