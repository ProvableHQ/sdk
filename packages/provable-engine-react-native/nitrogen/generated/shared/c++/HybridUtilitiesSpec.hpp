///
/// HybridUtilitiesSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ParsedRecordCiphertext` to properly resolve imports.
namespace margelo::nitro::shield { struct ParsedRecordCiphertext; }
// Forward declaration of `TokenRegistryBalance` to properly resolve imports.
namespace margelo::nitro::shield { struct TokenRegistryBalance; }

#include "ParsedRecordCiphertext.hpp"
#include <string>
#include <vector>
#include "TokenRegistryBalance.hpp"
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::shield {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Utilities`
   * Inherit this class to create instances of `HybridUtilitiesSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridUtilities: public HybridUtilitiesSpec {
   * public:
   *   HybridUtilities(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridUtilitiesSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridUtilitiesSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridUtilitiesSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual ParsedRecordCiphertext parseRecordCiphertext(const std::string& recordPlaintext) = 0;
      virtual std::string parseNativeAmount(const std::string& plaintext) = 0;
      virtual std::string parseTokenAmount(const std::string& plaintext, const std::string& programId, const std::string& tokenId) = 0;
      virtual std::string getTokenIdFromRecordPlaintext(const std::string& plaintext, const std::string& programId) = 0;
      virtual std::vector<std::string> constructTransactionInputs(const std::string& transferMethod, const std::string& programId, const std::string& tokenId, const std::string& to, const std::string& amount, const std::string& recordPlaintext) = 0;
      virtual std::vector<std::string> constructSplitTxInputs(const std::string& programId, const std::string& recordPlaintext, const std::string& amount) = 0;
      virtual std::string getHashedString(const std::string& idPlaintextBits) = 0;
      virtual std::string getMappingNameForProgram(const std::string& programId) = 0;
      virtual std::string parseU128(const std::string& balance) = 0;
      virtual std::string parseU64(const std::string& balance) = 0;
      virtual TokenRegistryBalance parseTokenRegistryBalance(const std::string& res) = 0;
      virtual bool verifyMnemonic(const std::string& mnemonic) = 0;
      virtual std::string cryptoBoxSealBase64(const std::string& publicKeyB64, const std::shared_ptr<ArrayBuffer>& message) = 0;
      virtual std::shared_ptr<ArrayBuffer> cryptoBoxSealOpenBase64(const std::string& publicKeyB64, const std::string& privateKeyB64, const std::string& sealedB64) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Utilities";
  };

} // namespace margelo::nitro::shield
