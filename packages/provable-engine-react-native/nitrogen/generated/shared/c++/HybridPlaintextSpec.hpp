///
/// HybridPlaintextSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridPlaintextSpec` to properly resolve imports.
namespace margelo::nitro::shield { class HybridPlaintextSpec; }

#include <memory>
#include "HybridPlaintextSpec.hpp"
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <vector>

namespace margelo::nitro::shield {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Plaintext`
   * Inherit this class to create instances of `HybridPlaintextSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPlaintext: public HybridPlaintextSpec {
   * public:
   *   HybridPlaintext(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPlaintextSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPlaintextSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPlaintextSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<HybridPlaintextSpec> fromString(const std::string& value) = 0;
      virtual std::shared_ptr<HybridPlaintextSpec> fromBytesLe(const std::shared_ptr<ArrayBuffer>& bytes) = 0;
      virtual std::shared_ptr<HybridPlaintextSpec> fromBitsLe(const std::shared_ptr<ArrayBuffer>& bits) = 0;
      virtual std::shared_ptr<HybridPlaintextSpec> fromFields(const std::vector<std::string>& fields) = 0;
      virtual std::shared_ptr<HybridPlaintextSpec> clone() = 0;
      virtual std::shared_ptr<HybridPlaintextSpec> find(const std::string& name) = 0;
      virtual std::shared_ptr<ArrayBuffer> toBytesLe() = 0;
      virtual std::shared_ptr<ArrayBuffer> toBitsLe() = 0;
      virtual std::vector<std::string> toFields() = 0;
      virtual std::string encrypt(const std::string& address, const std::string& randomizer) = 0;
      virtual std::string encryptSymmetric(const std::string& transitionViewKey) = 0;
      virtual std::string toObjectZero() = 0;
      virtual std::string plaintextType() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Plaintext";
  };

} // namespace margelo::nitro::shield
