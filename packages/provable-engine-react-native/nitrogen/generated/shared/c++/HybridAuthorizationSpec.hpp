///
/// HybridAuthorizationSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridAuthorizationSpec` to properly resolve imports.
namespace margelo::nitro::shield { class HybridAuthorizationSpec; }
// Forward declaration of `Transition` to properly resolve imports.
namespace margelo::nitro::shield { struct Transition; }
// Forward declaration of `HybridFieldSpec` to properly resolve imports.
namespace margelo::nitro::shield { class HybridFieldSpec; }
// Forward declaration of `AuthorizationJSON` to properly resolve imports.
namespace margelo::nitro::shield { struct AuthorizationJSON; }
// Forward declaration of `RequestJSON` to properly resolve imports.
namespace margelo::nitro::shield { struct RequestJSON; }
// Forward declaration of `TransitionJSON` to properly resolve imports.
namespace margelo::nitro::shield { struct TransitionJSON; }

#include <memory>
#include "HybridAuthorizationSpec.hpp"
#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include "Transition.hpp"
#include <vector>
#include "HybridFieldSpec.hpp"
#include "AuthorizationJSON.hpp"
#include "RequestJSON.hpp"
#include "TransitionJSON.hpp"

namespace margelo::nitro::shield {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Authorization`
   * Inherit this class to create instances of `HybridAuthorizationSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAuthorization: public HybridAuthorizationSpec {
   * public:
   *   HybridAuthorization(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAuthorizationSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAuthorizationSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAuthorizationSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<HybridAuthorizationSpec> replicate() = 0;
      virtual std::shared_ptr<HybridAuthorizationSpec> fromString(const std::string& authorizationString) = 0;
      virtual std::shared_ptr<ArrayBuffer> toBytesLe() = 0;
      virtual std::shared_ptr<HybridAuthorizationSpec> fromBytesLe(const std::shared_ptr<ArrayBuffer>& bytes) = 0;
      virtual bool isEqual(const std::shared_ptr<HybridAuthorizationSpec>& other) = 0;
      virtual double len() = 0;
      virtual bool isEmpty() = 0;
      virtual bool isFeePrivate() = 0;
      virtual bool isFeePublic() = 0;
      virtual bool isSplit() = 0;
      virtual void insertTransition(const Transition& transition) = 0;
      virtual std::vector<Transition> transitions() = 0;
      virtual std::shared_ptr<HybridFieldSpec> toExecutionId() = 0;
      virtual bool verify() = 0;
      virtual AuthorizationJSON toJSON() = 0;
      virtual std::vector<RequestJSON> getRequests() = 0;
      virtual std::vector<TransitionJSON> getTransitions() = 0;
      virtual std::shared_ptr<HybridAuthorizationSpec> fromRequestsAndTransitions(const std::vector<RequestJSON>& requests, const std::vector<TransitionJSON>& transitions) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Authorization";
  };

} // namespace margelo::nitro::shield
