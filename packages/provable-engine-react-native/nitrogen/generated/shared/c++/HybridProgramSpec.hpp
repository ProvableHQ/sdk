///
/// HybridProgramSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridProgramSpec` to properly resolve imports.
namespace margelo::nitro::shield { class HybridProgramSpec; }

#include <memory>
#include "HybridProgramSpec.hpp"
#include <string>
#include <vector>

namespace margelo::nitro::shield {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Program`
   * Inherit this class to create instances of `HybridProgramSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridProgram: public HybridProgramSpec {
   * public:
   *   HybridProgram(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridProgramSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridProgramSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridProgramSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<HybridProgramSpec> fromString(const std::string& source) = 0;
      virtual std::shared_ptr<HybridProgramSpec> clone() = 0;
      virtual bool hasFunction(const std::string& functionName) = 0;
      virtual std::vector<std::string> getFunctions() = 0;
      virtual std::string getFunctionInputs(const std::string& functionName) = 0;
      virtual std::string getMappings() = 0;
      virtual std::string getRecordMembers(const std::string& recordName) = 0;
      virtual std::string getStructMembers(const std::string& structName) = 0;
      virtual std::vector<std::string> getImports() = 0;
      virtual std::string id() = 0;
      virtual std::string address() = 0;
      virtual bool isEqual(const std::shared_ptr<HybridProgramSpec>& other) = 0;
      virtual void getCreditsProgram() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Program";
  };

} // namespace margelo::nitro::shield
