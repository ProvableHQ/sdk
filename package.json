{
  "name": "sdk-root",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "sdk",
    "wasm",
    "packages/*",
    "website",
    "create-leo-app",
    "create-leo-app/*",
    "e2e",
    "e2e/*"
  ],
  "type": "module",
  "scripts": {
    "build:packages": "yarn workspace @provablehq/provablekit build && yarn workspace @provablehq/provable-engine-wasm build && yarn workspace @provablehq/provable-engine-react-native build",
    "build:wasm": "rustup show active-toolchain && cd wasm && yarn build",
    "build:sdk": "cd sdk && yarn build",
    "build:sdk-docs": "sh scripts/jsdoc.sh",
    "build:create-leo-app": "cd create-leo-app && yarn build",
    "build:all": "yarn build:wasm && yarn build:packages && yarn build:sdk && yarn build:create-leo-app",
    "build:wasm-debug": "cd wasm && BUILD_DEBUG=1 yarn build",
    "build:all-debug": "yarn build:wasm-debug && yarn build:packages && yarn build:sdk && yarn build:create-leo-app",
    "start:website": "cd website && yarn dev",
    "test:packages": "yarn workspace @provablehq/provablekit test && yarn workspace @provablehq/provable-engine-wasm test && yarn workspace @provablehq/provable-engine-react-native test",
    "test:migration-guards": "node scripts/migration-guards.mjs",
    "test:wasm": "cd wasm && yarn test",
    "test:sdk": "cd sdk && yarn test",
    "test": "yarn test:wasm && yarn test:packages && yarn test:sdk",
    "change-version": "node scripts/change-version.js",
    "deploy:wasm": "cd wasm && npm publish --access=public",
    "deploy:provablekit": "cd packages/provable-core && npm publish --access=public",
    "deploy:provable-engine-wasm": "cd packages/provable-engine-wasm && npm publish --access=public",
    "deploy:provable-engine-react-native": "cd packages/provable-engine-react-native && npm publish --access=public",
    "deploy:sdk": "cd sdk && npm publish --access=public",
    "deploy:create-leo-app": "cd create-leo-app && npm publish --access=public",
    "deploy": "yarn build:all && yarn deploy:wasm && yarn deploy:provablekit && yarn deploy:provable-engine-wasm && yarn deploy:provable-engine-react-native && yarn deploy:sdk && yarn deploy:create-leo-app",
    "lint": "prettier . --check",
    "pretty": "prettier . --write"
  },
  "optionalDependencies": {
    "glob": "^11.0.1"
  },
  "devDependencies": {
    "prettier": "3.4.2",
    "wasm-pack": "^0.13.1"
  }
}
